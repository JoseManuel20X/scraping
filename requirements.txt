# Dependencias para el scraping
selenium==4.10.0
webdriver-manager==3.8.0
beautifulsoup4==4.12.0
requests==2.28.2

# Dependencias para interactuar con PostgreSQL
psycopg2-binary==2.9.5

# Dependencias para manejar variables de entorno
python-dotenv==1.0.0

# Dependencias para el uso de OpenAI
openai==0.27.0

# Dependencias para programar el scraper (APScheduler)
apscheduler==3.9.1

# Dependencias para logging (usada por defecto en Python)
# No es necesario agregar nada extra si solo se usa el logging básico
